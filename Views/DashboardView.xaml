<UserControl x:Class="RecallFinanceApp.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Portföy Bakiyesi" FontSize="16" Foreground="#666"/>
            <TextBlock Text="{Binding DataContext.Balance, RelativeSource={RelativeSource AncestorType=Window}, StringFormat='{}{0:C2}'}" 
                       FontSize="42" FontWeight="Bold" Foreground="#2E7D32"/>
        </StackPanel>

        <Button Grid.Row="1" Content="Bakiyeyi Manuel Güncelle" 
                Click="Update_Click" Padding="20,10" Background="#1976D2" Foreground="White"/>

        <Grid Grid.Row="2" Margin="0,20,0,0">
            <TextBlock Text="İşlem Geçmişi" FontWeight="Bold" Margin="0,0,0,5"/>
            <DataGrid ItemsSource="{Binding DataContext.Transactions, RelativeSource={RelativeSource AncestorType=Window}}" 
                      Margin="0,25,0,0" Background="White" AutoGenerateColumns="False" HeadersVisibility="None">
                <DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding .}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>